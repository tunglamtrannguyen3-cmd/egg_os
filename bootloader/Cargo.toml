[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = "0.28"
common = { path = "../common" }
uefi-services = "0.25"
log = "0.4"

[profile.release]
opt-level = "z"
panic = "abort"
lto = true
